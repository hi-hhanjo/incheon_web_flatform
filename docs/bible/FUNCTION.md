# 기능 명세서: 인천 유나이티드 응원가 웹페이지

- **문서 버전**: v1.1
- **작성일**: 2026-07-11 (v1.1 업데이트: 2026-07-13)
- **연관 문서**: PRD.md (v1.1)
- **목적**: PRD에 정의된 각 기능이 **화면에서 실제로 어떻게 동작하는지**를 세부 상황(정상/예외/빈 상태)까지 정의하여, 개발(AI 코드 생성 포함) 시 빈틈이 생기지 않도록 합니다.

---

## 기능 목록 한눈에 보기

| ID | 기능명 | 우선순위 | 상태 |
|------|--------|----------|------|
| F-01 | 응원가 목록 화면 | MVP (필수) | 명세 완료 |
| F-02 | 응원가 상세 화면 | MVP (필수) | 명세 완료 |
| F-03 | 모바일 친화 레이아웃 | MVP (필수) | 명세 완료 |
| F-04 | 영상 종류 선택 (공식/현장) | MVP (필수) | 명세 완료 |
| F-11 | 검색 기능 | 확장 1순위 | 미착수 |

> 이 문서는 MVP 기능(F-01~F-04)을 다룹니다. 확장 기능은 별도 명세로 관리합니다.

---

## 명세서 읽는 법
각 기능은 아래 항목으로 정의됩니다.
- **개요**: 이 기능이 하는 일 한 줄 요약
- **화면 구성 요소**: 화면에 보이는 부품들
- **동작 흐름**: 사용자가 어떤 행동을 하면 무슨 일이 일어나는가
- **상태별 처리**: 정상 / 예외 / 빈(데이터 없음) 상황
- **완료 조건**: 이 기능이 "다 됐다"고 볼 수 있는 체크리스트

---

## F-01. 응원가 목록 화면

### 개요
- 인천 유나이티드 응원가들을 카드 목록으로 보여주고, 하나를 선택하면 상세 화면으로 넘겨줍니다.

### 화면 구성 요소
- **상단 제목**: 예) "인천 유나이티드 응원가"
- **정렬 토글**: [기본] [가나다순] 세그먼트 버튼 — 목록 정렬 방식을 전환 (곡이 1개 이상일 때만 표시)
- **응원가 카드 목록**: 세로로 나열되는 카드들
  - 카드에 표시: 응원가 **제목** (필수)
  - (선택) '대표곡' 표시(배지) — 있으면 눈에 띄게

### 동작 흐름
1. 사용자가 웹에 접속한다.
2. `songs.json`의 응원가 데이터를 읽어 목록을 그린다.
3. **기본 정렬**: 데이터에 입력된 순서(등록 순서) 그대로 표시한다. *(v1.1 — 이전의 '대표곡 우선 정렬'은 대표곡 판단 기준의 주관성 문제로 제외)*
4. **정렬 전환**: 사용자가 '가나다순' 토글을 누르면 제목 기준 가나다순으로 바뀌고, 다시 '기본'을 누르면 원래 순서로 돌아온다.
5. 사용자가 카드를 탭하면 → 해당 곡의 **상세 화면(F-02)** 으로 이동한다. (곡의 `id`를 넘긴다)

### 상태별 처리
- **정상**: 곡이 1개 이상 있으면 정렬 토글과 카드 목록을 보여준다.
- **빈 상태**: 곡이 하나도 없으면 "등록된 응원가가 없습니다" 문구만 보여준다. (정렬 토글은 표시하지 않는다)
- **로딩 중**: 데이터를 읽는 동안 간단한 로딩 표시(예: "불러오는 중...")를 보여준다.

### 완료 조건 (체크리스트)
- [ ] 응원가 제목이 카드에 정확히 표시된다.
- [ ] 기본 진입 시 등록 순서 그대로 표시된다.
- [ ] '가나다순' 토글을 누르면 제목 기준으로 정렬이 바뀌고, '기본'을 누르면 원래대로 돌아온다.
- [ ] 카드를 누르면 올바른 곡의 상세 화면으로 이동한다.
- [ ] 곡이 없을 때 빈 상태 문구가 나오고, 이때는 정렬 토글이 보이지 않는다.

---

## F-02. 응원가 상세 화면

### 개요
- 선택한 응원가 하나에 대해 **영상(공식/현장 선택 가능) + 가사**를 보여줍니다.

### 화면 구성 요소 (위에서부터 순서대로)
1. **뒤로 가기 버튼**: 목록 화면으로 복귀
2. **응원가 제목**
3. **영상 선택 탭**: [공식 영상] [현장 영상] (F-04에서 상세 정의)
4. **영상 영역**: 선택된 유튜브 영상 (임베드)
5. **가사 영역**: 영상 아래에 가사 표시 (줄바꿈 그대로 반영)

### 동작 흐름
1. 목록에서 넘어온 곡 `id`로 해당 곡 데이터를 찾는다.
2. 화면을 **영상 → 가사** 순서로 그린다.
3. 영상은 기본값(공식 영상)을 먼저 보여준다. (F-04)
4. 사용자가 '뒤로 가기'를 누르면 목록 화면으로 돌아간다.

### 상태별 처리
- **정상**: 영상과 가사가 모두 있으면 순서대로 표시한다.
- **가사 없음**: 가사가 비어 있으면 "가사 준비 중입니다" 문구를 보여준다.
- **잘못된 접근**: 존재하지 않는 곡 `id`로 접근하면 "해당 응원가를 찾을 수 없습니다" 문구와 목록으로 가는 버튼을 보여준다.

### 완료 조건 (체크리스트)
- [ ] 화면이 영상 → 가사 순서로 표시된다.
- [ ] 가사의 줄바꿈이 원본대로 보인다.
- [ ] 뒤로 가기로 목록에 정상 복귀한다.
- [ ] 없는 곡으로 접근 시 안내 문구가 나온다.

---

## F-04. 영상 종류 선택 (공식 / 현장)

### 개요
- 한 곡에 여러 영상(공식/현장 등)이 있을 때, 탭으로 하나를 골라 볼 수 있게 합니다.

### 화면 구성 요소
- **영상 선택 탭**: 곡의 `videos` 목록을 읽어 탭 버튼을 자동 생성
  - 예: `videos`에 official, live 2개가 있으면 → [공식 영상] [현장 영상] 2개 탭
- **선택 표시**: 현재 선택된 탭이 시각적으로 구분됨(강조)

### 동작 흐름
1. 상세 화면 진입 시, `videos` 목록의 **첫 번째 영상(기본: 공식 영상)** 을 선택 상태로 시작한다.
2. 사용자가 다른 탭(예: 현장 영상)을 누르면 → 해당 영상으로 **교체**한다.
3. 이때 **선택된 영상 1개만 불러온다.** (나머지는 불러오지 않아 로딩 부담을 줄임)

### 상태별 처리
- **영상 2개 이상**: 탭을 여러 개 보여준다.
- **영상 1개**: 탭을 만들지 않거나 1개만 보여주고, 그 영상을 바로 표시한다. (탭이 무의미하게 1개만 뜨는 걸 방지)
- **영상 0개**: "영상 준비 중입니다" 문구를 보여준다. (가사는 그대로 표시)

### 완료 조건 (체크리스트)
- [ ] `videos` 개수에 맞춰 탭이 자동 생성된다.
- [ ] 진입 시 기본값(공식 영상)이 먼저 보인다.
- [ ] 탭을 누르면 영상이 정확히 교체된다.
- [ ] 선택되지 않은 영상은 불러오지 않는다.
- [ ] 영상이 1개거나 0개인 경우가 자연스럽게 처리된다.

---

## F-03. 모바일 친화 레이아웃

### 개요
- 스마트폰 세로 화면을 기준으로, 모든 화면이 편하게 보이고 눌러지게 합니다.

### 세부 규칙
- **영상 크기**: 화면 너비에 맞춰 자동으로 조절되며, 가로세로 비율(16:9)을 유지한다.
- **터치 영역**: 카드·탭·버튼은 손가락으로 누르기 편한 크기(권장 최소 44px 높이)로 만든다.
- **글자 크기**: 가사는 야외에서도 읽히도록 충분히 크게(본문 권장 16px 이상).
- **가로 스크롤 금지**: 어떤 화면에서도 좌우로 밀리는 현상이 없어야 한다.

### 상태별 처리
- **화면 크기 변화**: 태블릿·PC에서 봐도 깨지지 않게 콘텐츠 폭을 적절히 제한한다.

### 완료 조건 (체크리스트)
- [ ] 스마트폰에서 영상이 화면 폭에 꽉 맞게 보인다.
- [ ] 버튼·탭이 누르기 편한 크기다.
- [ ] 가로 스크롤이 발생하지 않는다.
- [ ] 태블릿·PC에서도 레이아웃이 깨지지 않는다.

---

## 공통 데이터 규칙 (참고)

기능들이 공통으로 사용하는 데이터 형식입니다. (PRD 6번과 동일)

```json
{
  "id": 1,
  "title": "응원가 제목",
  "videos": [
    { "type": "official", "label": "공식 영상", "youtubeId": "dQw4w9WgXcQ" },
    { "type": "live",     "label": "현장 영상", "youtubeId": "abcd1234EFG" }
  ],
  "lyrics": "응원가 가사\n(줄바꿈 포함)",
  "category": "팀 응원가",
  "tags": ["대표곡"],
  "isFavorite": false
}
```

- **`tags`에 '대표곡'** → F-01 목록 정렬에서 위쪽 배치
- **`videos` 목록** → F-04 탭 자동 생성의 기준
- **`lyrics`** → F-02 가사 영역에 줄바꿈 그대로 표시

---

## 전체 완료 기준 (MVP 통과 조건)

아래가 모두 만족되면 MVP가 완성된 것으로 봅니다.
1. 목록 → 상세 → 목록으로 이동이 매끄럽게 된다. (F-01, F-02)
2. 상세 화면에서 공식/현장 영상을 탭으로 바꿔 볼 수 있다. (F-04)
3. 영상·가사가 없는 예외 상황에서도 화면이 깨지지 않는다.
4. 스마트폰에서 편하게 보고 누를 수 있다. (F-03)

---

## 변경 이력 (Changelog)

| 버전 | 날짜 | 내용 |
|------|------|------|
| v1.1 | 2026-07-13 | F-01 정렬 규칙 변경: '대표곡 우선 정렬'(주관적 판단 개입 문제) 제거 → 기본은 등록 순서, 사용자가 '가나다순' 토글로 전환 가능하도록 수정 |
| v1.0 | 2026-07-11 | PRD v1.1 기반 MVP 기능(F-01~F-04) 상세 명세 최초 작성 |
