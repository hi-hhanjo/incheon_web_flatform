# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
# python (scraper/)
__pycache__/
.venv/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# 응원가 검수 큐 — 크롤러(export_chant_candidates.py)가 만들고 promote_candidates.py가 비우는
# 작업용 파일. 승격이 끝나면 항상 []이라 커밋해도 빈 파일만 남는다. 내용은 언제든 재생성 가능
# (앱은 이 파일을 읽지 않는다 — CRAWLER_SPEC.md 3.7).
data/songs-candidates.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
